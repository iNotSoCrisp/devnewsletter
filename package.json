{
  "name": "lastscroll",
  "private": true,
  "version": "1.0.0",
  "description": "LastScroll — Dev newsletter platform (monorepo)",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=frontend",
    "start": "npm run start --workspace=backend"
  }
}
